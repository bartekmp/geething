// Copy this file to credentials.js and fill in your values.
// credentials.js is gitignored — never commit real values.
export const CLIENT_ID = 'YOUR_CLIENT_ID.apps.googleusercontent.com';

// Client secret for the Web application OAuth client type.
// Not truly secret in an extension (code is readable), but required by Google.
export const CLIENT_SECRET = 'YOUR_CLIENT_SECRET';
